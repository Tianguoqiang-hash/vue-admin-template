import * as attr from '@/api/attr'
import * as tradeMark from '@/api/tradeMark'
import * as spu from '@/api/spu'
import * as sku from '@/api/sku'
import * as  user from '@/api/user'

export default {
    attr,
    tradeMark,
    spu,
    sku,
    user
}